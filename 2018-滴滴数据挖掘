1.寻找丑数
把只包含因子2、3和5的数称作丑数（Ugly Number）。例如6、8都是丑数，但14不是，因为它包含因子7。 习惯上我们把1当做是第一个丑数。求按从小到大的顺序的第N个丑数。

输入描述:
整数N
输出描述:
第N个丑数

示例1
输入
6
输出
6

#include <bits/stdc++.h>
using namespace std;
 
bool isTest(int data)
{
    while(data>0&&data%2==0)
    {
        data/=2;
    }
    while(data>0&&data%3==0)
    {
        data/=3;
    }
    while(data>0&&data%5==0)
    {
        data/=5;
    }
 
    if(data==1)
    {
        return true;
    }
    else
    {
        return false;
    }
}
 
int main()
{
    vector<int> number;
    int n;
    cin>>n;
    int data=1;
    while(number.size()<n)
    {
        if(isTest(data))
        {
            number.push_back(data);
        }
        ++data;
    }
    cout<<number[n-1];
 
    return 0;
}


2.给出n个数字 a_1,...,a_n，问最多有多少不重叠的非空区间，使得每个区间内数字的xor都等于0。



3.Hash算法都不可还原，SHA-1属于Hash
5.outer是产生并集 inner是交际。right是以右边为标准左边带空。left是以左边为标准右边带空（sql查询语句）
